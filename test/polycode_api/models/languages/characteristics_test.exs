defmodule PolycodeApi.Models.Languages.CharacteristicsTest do
  use ExUnit.Case
  doctest PolycodeApi.Models.Languages.Characteristics

  test "greets the world" do
    assert PolycodeApi.soma(1,2) == 3
  end
end
