defmodule PolycodeApi.Models.Languages.IntegrationsTest do
  use ExUnit.Case
  doctest PolycodeApi.Models.Languages.Integrations

  test "greets the world" do
    assert PolycodeApi.soma(1,2) == 3
  end
end
