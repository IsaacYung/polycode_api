defmodule PolycodeApi.Models.Languages.SynataxesTest do
  use ExUnit.Case
  doctest PolycodeApi.Models.Languages.Synataxes

  test "greets the world" do
    assert PolycodeApi.soma(1,2) == 3
  end
end
